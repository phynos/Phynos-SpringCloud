spring:
  application:
    name: spring-cloud-product-user
  profiles:
    # 必须在这里配置，不然无法从配置中心获取信息
    active: dev

---
spring:
  cloud:
    config:
      uri: http://localhost:6300
      label: master
      profile: prod
  config:
    activate:
      on-profile:


---
spring:
  cloud:
    config:
      #uri: http://localhost:6300
      # 从注册中心获取配置中心的服务
      discovery:
        enabled: true
        service-id: charger-config
      label: master
      profile: dev
  config:
    activate:
      on-profile: